## TO DO:

- Input from user is stored in state from event.target.value
- We need to fetch api data when user clicks on button
  - Create async onClick function
- Create component for the movie that user searches for
  - Also create path (ex. discover/:movieTitle)
  - Display movie data there
